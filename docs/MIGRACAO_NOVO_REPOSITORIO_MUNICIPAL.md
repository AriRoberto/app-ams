# Migração para novo repositório do App Municipal

## Nome sugerido
CidadeAtende

## Próximos passos
1. Criar repositório remoto `app-ams` no GitHub.
2. Configurar remote:
   - `git remote set-url origin https://github.com/AriRoberto/app-ams.git`
3. Publicar:
   - `git push -u origin main`
